{
  "name": "ciaobang",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter platform dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@11.1.0",
  "engines": {
    "node": ">=20.9.0"
  }
}
