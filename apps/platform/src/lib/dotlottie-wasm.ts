// Self-hosted copy of @lottiefiles/dotlottie-web's renderer wasm (the player
// otherwise fetches it from cdn.jsdelivr.net — an extra third-party origin).
// The file lives in /public/dotlottie with the package version in its name:
// bump the filename when upgrading the package (immutable cache headers).
export const DOTLOTTIE_WASM_URL = "/dotlottie/dotlottie-player-0.75.0.wasm";
